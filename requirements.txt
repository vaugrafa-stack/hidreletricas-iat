# Dependências do DASHBOARD (usadas no deploy — ex.: Streamlit Community Cloud).
# Mantido enxuto de propósito: NÃO inclua 'arcgis' aqui (pacote gigante que estoura
# o build da nuvem). As libs do pipeline/ETL ficam em requirements-pipeline.txt.
streamlit>=1.35.0
streamlit-folium>=0.20.0
folium>=0.17.0
plotly>=5.22.0
pandas>=2.0.0
pyyaml>=6.0.1
